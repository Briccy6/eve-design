#include<iostream>
#include<cmath>
using namespace std;
int main(){
int a,b,c ;
cout<<(eter a,b,c);
cin>> a,b,c;     
delta=b*b-4*a*c;    
if delta>0 then 
{
    x1=(-b+sqrt(delta))/(2*a);
    x2=(-b-sqrt(delta))/(2*a);
    write x1,x2;
}
else if delta=0 then 
{
    x=-b/(2*a);
    cout<< x;
}
else 
{
    cout<< "No solution";
}
return0;}